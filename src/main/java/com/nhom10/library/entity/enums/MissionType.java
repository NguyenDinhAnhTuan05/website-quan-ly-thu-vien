package com.nhom10.library.entity.enums;

public enum MissionType {
    DAILY,      // Nhiệm vụ lặp lại mỗi ngày (ví dụ: Check-in)
    ONE_TIME,   // Nhiệm vụ chỉ làm một lần (ví dụ: Đọc cuốn sách đầu tiên)
    MILESTONE   // Nhiệm vụ theo mốc tích lũy (ví dụ: Viết 10 reviews)
}
